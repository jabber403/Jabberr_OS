{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build OS",
            "type": "shell",
            "command": "cargo build --target x86_64-unknown-none",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}